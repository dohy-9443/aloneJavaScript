const array = [0, 2, 3, 4, 5, 6, 7, 8, 9];

const output = array.map((value) => value * value);

console.log(output);

const cartList = {
  a: 1,
  b: 2,
  c: 3,
};
const cartLength = Object.keys(cartList).length;

console.log(cartLength);
